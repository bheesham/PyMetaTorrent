- complete the bots
    - make sure that the parsing won't overwrite any parsing previously done, just add to it
	^ by add to it, i mean update it
- make the bots aware of each other via a table in the database
- keep statistics on the bot's activities
- work on the website frontend and backend
- use web.py (http://webpy.org) coupled with another package as the API server software
- set up a load balancing MySQL setup
    - test the limits, surpass them
- be a boss